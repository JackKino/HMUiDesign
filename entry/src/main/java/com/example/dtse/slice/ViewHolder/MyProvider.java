package com.example.dtse.slice.ViewHolder;

import com.chg.ultimateprovider.UltimateProvider2;
import ohos.app.Context;

import java.util.List;

public class MyProvider extends UltimateProvider2 {
    /**
     * 构造方法
     *
     * @param models  模型数据
     * @param context context
     */
    public MyProvider(List models, Context context) {
        super(models, context);
    }


}
